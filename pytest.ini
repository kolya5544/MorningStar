[pytest]
testpaths = server/tests
pythonpath = server
markers =
    unit: fast deterministic tests for isolated business logic
    integration: API and persistence tests against the test database
    e2e: end-to-end tests that exercise the full stack
addopts = --strict-markers --cov=server/app --cov-report=term-missing --cov-fail-under=75

